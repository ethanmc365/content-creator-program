# Where this got to, and what is still open

Written 31 Aug 2026, at the end of a long session. This is the working list for
whoever picks it up next, me included. Everything under "Done" is on `main` and
deployed; everything under "Open" is not started unless it says otherwise.

Read this with `docs/` and the memory files, not instead of them.

---

## Verification status, honestly

Everything below that is marked done passes lint, the 432-test suite and a
production build, and most of it was checked in a browser as it was built.

**The CAPTCHA problem is solved** - see `vite-dev-auth.js` and the note further
down. Signed-in local testing works again, and the second pass on this file's
open list was done against the real database at 375px rather than by reading.

**Still without a final visual pass:** the **rebuilt photo board** (free move +
corner resize). Its layout maths is unit-tested (`PhotoBoard.test.js`, including
a no-overlap assertion) and the earlier masonry version was verified by driving
real pointer events, but the free-drag build itself has not been driven end to
end in a browser. **Check that first.**

The reaction pill has since been rebuilt again (hover intent, no shadow) and
verified - see "Done since this file was written".

---

## Done and deployed

### The cutover
- The network is the app. `NetworkRoute` and `CommunityContext` no longer gate
  on the preview flag; every creator is on the worldwide shell.
- Nav is five tabs: **Worldwide** (globe), Challenges, Rooms, DMs, Calendar.
  `/home` and `/chat/:channel` redirect rather than 404.
- Header wordmark is "Content Creator Community".
- DMs and Calendar left the avatar menu (they are primary tabs).

### Security
- **Migration 149 closed a real hole.** `messages` had one SELECT policy,
  `is_member()`, which only means "are you an active profile" - so every creator
  could read every channel, including the Worldwide staff room and every market
  they are not in. Verified fixed against a plain UK creator: staff 0, Spain 0,
  Worldwide 157, UK 7. Nothing had leaked; the staff room was empty.
- 30 legacy rows were backfilled with `community_id`/`channel_id` first, so the
  policy needs no "or null" escape hatch.

### Rooms
- **Posting works again.** An `isLiveWorldwide` guard from the preview era made
  Worldwide's General, Announcements and Content tips read-only. Removed.
- Own messages are right-aligned orange bubbles; everybody else's are left. The
  "my message looks like someone else sent it" report was this: it was a flat
  Slack-style log where every row was drawn identically.
- Reply, delete (own, or any as admin - migration 147), edit, report, reactions.
- **Headings are 20/17/15 against a 14px body.** They were 16/14/14, so an H2
  was the same size as a paragraph. Renderer and composer both changed.
- Content tips room added to every market (migration 148), and `create_market`
  updated so new markets get one.
- Market scope disclaimers removed from four surfaces.
- Rooms index is drag-reorderable on mobile, sharing `rooms-market-order` with
  the desktop sidebar.

### Fixed bugs
- **UK market overview loaded nothing.** `standings.filter(s => !s.profiles.is_test)`
  - `views_leaderboard` returns flat columns, so there is no `profiles` object.
  It threw, `load()` rejected, `setLoading(false)` never ran. Only the UK showed
  it because every other market returns zero rows today. `load()` now has a catch.
- **Challenge archive view counts.** `results.final_views` is a snapshot taken
  when an admin saves results; views keep syncing off each entry's link forever
  after. One creator had drifted ~20k. The archive reads live views now and totals
  every entry, not just the ranked ones. **Ranks untouched** - that challenge is
  `best_video`, so the order is a human decision that was paid out.
- Countries appeared twice on Edit profile (WorldMap drew its own chip list).
- Adding/deleting a photo did not update the board (`lib/photoEvents`).

### Profile
Header (role beside name, age below, bigger photo), coloured named social links,
LinkedIn column (migration 146), Languages rename, bucket icon, borderless trip
rows, orange local-time icon, no Team-only card, no country list under the map,
no share card, "headed next" reads upcoming flights too, bucket list expands,
Milestones rename, flight log in the rail, travel games shows streaks off a new
pure RPC (migration 145 - `my_game_streak` WRITES and must never be called to
draw a profile), showcase sorted by views with dates past three days.

### Edit profile
Tab deep-links (`?tab=photos`), no tab hints, growing About and quote boxes,
searchable language picker, removable country chips, dial-code menu fixed,
save bar cleaned up.

---

## Open, roughly in the order I would do them

**Re-audited 31 Aug 2026, later the same day.** A lot of the list below had
already been done by the commits that landed after this file was written, and
the rest of it was worked through in one pass. What follows is what is ACTUALLY
left. Everything struck from the list was checked in a browser at 375px against
the real database, not assumed.

### Done since this file was written

- **Chat action bar**: hover intent (it waits a second on one message before
  opening, so scrolling a thread is silent now), a rise-and-fade instead of a
  flat unfold, and no shadow on the pill - the shadow was being clipped square
  by the 0fr box and reading as "a grey box around the oval card". Touch is
  gated out of the hover path entirely by a new `hoverable` Tailwind variant.
- **DM bubbles shrink to fit.** They were blocks in a `w-full` column, so every
  bubble was 80% of the thread whatever was in it.
- **Worldwide on a phone**: the market switcher is a full-width bar and has
  taken over from "Your markets", which is gone on mobile. The stats card, the
  one-line greeting, the announcement cards with the 15-day cutoff, the map
  hint and the on-the-move button were already done.
- **Milestones on mobile**: no overlap, and the order is already route → where
  you are → stats. Verified by measuring every card's box.
- **Notification centre on mobile**: full-width panel, and the unread dot moved
  to the left of the row away from the dismiss cross. Both already done.
- **Collab board**: no country chips under the map at either width, no legend.
- **Market pages open full screen** (`allowFullscreen` is its own prop now).
- **"Your markets" is a pin, not a flag.**
- **Challenges total is in euros, rounded to the nearest ten.**
- **Settings has no subtitle.**
- **The money is in euros, and CONVERTED rather than relabelled.** This was
  worse than the note in this file suggested. `formatMoney` defaulted to GBP
  and half a dozen surfaces called it with no currency at all over figures
  already converted into euros, so the admin panel showed EUR 190 of spend
  wearing a pound sign. Admin analytics was worse again: its money tiles were
  raw sums over a table holding both currencies, and the EUR/GBP toggle changed
  only the symbol - "£190" and "€190" were the same number. Admin rewards and
  the dashboard were adding pounds to euros as plain numbers. All of them now
  go through `convert`/`rewardsTotal`, which is what Rewards.jsx already did.
  Per-ROW amounts still show what was actually paid, in the currency it was
  paid in; only totals are converted, and a converted total is marked "≈".
- **Admin on mobile**: the analytics tabs scroll sideways instead of wrapping
  into three rows with the underline under only the last of them; the Arrange
  button is smaller and on the heading's line (`inlineAction` on PageHeader,
  opt-in - most pages want their action stacked on a phone); testing centre
  cards are titles, not paragraphs.
- **The local CAPTCHA hang is solved.** `vite-dev-auth.js` serves
  `/__dev-login` on the dev server only (`apply: 'serve'`, so it cannot be
  built), mints a session with the admin API for `@trypcreators.test` addresses
  only, and needs `SUPABASE_SERVICE_KEY` in the gitignored `.env.qa`. Use
  `?as=admin` and `?to=/some/path`.

### Still open

#### 1. Flight log (§4 of the old list, untouched)
Every item still stands: the OpenFlights credit line, clicking an airport dot,
the flicker on zoom, the tiny trip photo, clicking a logged flight, return
trips as one entry, always showing flights from the home airport, the black
boarding-pass camera on mobile, and the 404s.

#### 2. Games on mobile (§5 of the old list, untouched)
The Guess-the-Flight card size, opening a puzzle centred, the guess bar above
the keyboard, and the Languages game's scrolling. These want a real phone -
the guess bar in particular is about the visual viewport and the home-screen
inset, which an emulated viewport does not reproduce honestly.

#### 3. Creator maps on profiles have no full-screen button
Zoom buttons are there. Full screen is not: the profile uses `WorldMap`, which
is a different component from `CreatorMap` and does not have the portal-based
full-screen machinery. Porting it is the job.

#### 4. Not verified, not attempted
- **The full-screen map rotate animation being laggy.** The pin shadows and the
  `Countries` memo already fixed most of the full-screen lag; the rotate
  transition itself is `screen.orientation.lock` and needs a real device.
- **Live challenge cards more condensed on mobile.** No market had a live
  challenge while this was being checked, so there was nothing to look at.
- **Polls, games and resources in the new rooms.** Not exercised.
- **The referral voucher amount.** The note in this file does not say what is
  wrong with it, and nothing obvious is - it needs a sentence from Ethan.

#### 5. Two things worth knowing about local testing
- **A stale service worker on localhost causes a reload loop.** A prod SW
  registered at `localhost:5173` from an earlier session served cached assets
  and the page reloaded forever. Unregister it and clear `caches` once per
  browser profile.
- **The QA accounts are `is_sandbox`, and a trigger stops them posting**
  (`sandbox_cannot_speak`). There is a seeded QA-to-QA DM thread with messages
  of varying length, which is what the bubble-width fix was checked against.

---

## Things worth not relearning

- **`my_game_streak` WRITES.** It spends freezes. Never call it to draw a
  profile. `profile_game_stats` (145) is the pure read.
- **`results.final_views` is a snapshot and drifts.** Any surface showing views
  should read `submissions.logged_views`.
- **`is_member()` is not membership.** It means "active profile". Check
  `my_scopes()` for anything community-scoped.
- **Never retype a live function body.** `pg_get_functiondef` first. Migration
  148 copied `create_market` verbatim and edited four places.
- **Both revokes.** A definer function needs `revoke from public` AND
  `revoke from anon`; a safe one reads back as postgres/authenticated/service_role.
- **Drag listeners belong on pointerdown, not in an effect** keyed on drag
  state - an effect's listeners are not live until React commits, so the first
  move and sometimes the pointerup are lost. This bit the photo board and the
  boarding-pass camera.
- **The local Turnstile CAPTCHA currently hangs**, which blocks signed-in local
  testing. Worth fixing before the next session.
- QA logins are in the memory file; they are the sanctioned testing exception.
